package practicoIII;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

public class ejercicio1 {

	public static void main(String[] args) {
		
		Persona per1 = new Persona("Monica Lopez");
		Persona per2 = new Persona("Maria Mamani");
		Persona per3 = new Persona("Lucrecia Rossi");
		Persona per4 = new Persona("Micaela Correa");
		Persona per5 = new Persona("Julio Prado");
		Persona per6 = new Persona("Marcelo Fuertes");
		
		
		List<Persona> lPersona = new ArrayList<Persona>();
		lPersona.add(per1);
		lPersona.add(per2);
		lPersona.add(per3);
		lPersona.add(per4);
		lPersona.add(per5);
		lPersona.add(per6);
		
		List<String> personaString = lPersona.stream().map((pString) -> pString.getNomApell().toUpperCase()).collect(Collectors.toList());
		
		System.out.println("Listado de elementos de la coleccion en formato String: ");
		personaString.stream().forEach((per) -> System.out.println(per));
		
		System.out.println("Listado de elementos de la coleccion en formato Persona: ");
		lPersona.stream().forEach((b)->System.out.println(b.getNomApell()));
	}

}